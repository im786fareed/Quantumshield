'use client';

import SIMProtection from '@/components/SIMProtection';
import PageWrapper from '../_shared/PageWrapper';

export default function simprotectionPage() {
  return (
    <PageWrapper>
      <SIMProtection lang="en" />
    </PageWrapper>
  );
}
